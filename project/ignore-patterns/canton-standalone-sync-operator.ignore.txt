# With a zero base rate, a member's topology broadcasts bounce off the splitwell sequencer
# until its traffic purchase is granted. The participant retries them automatically. The
# outbox line covers both the store-based ("flusher failed") and queue-based ("flusher")
# wordings.
Failed broadcasting topology transactions.*SEQUENCER_NOT_ENOUGH_TRAFFIC_CREDIT
synchronizer outbox flusher.*splitwell.*failed the following topology transactions
